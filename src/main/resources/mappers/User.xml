<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springjwt_maven.repository.UserDao">

    <select id="existsByUsername" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findByUsername" parameterType="String" resultType="com.example.springjwt_maven.entity.UserEntity">
        select id, username, password, role
        from users
        where username = #{username}
<!--        and password = #{password}-->
    </select>

    <insert id="registUser" parameterType="com.example.springjwt_maven.entity.UserEntity">
        insert into users
        values (id,#{username},#{password},"ROLE_ADMIN")
    </insert>
</mapper>